@model IEnumerable<Ranking_Estudiantil.Models.AcademicUnity>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AcademicUnityName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AcademicUnityName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AcademicUnityID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AcademicUnityID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AcademicUnityID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
