@model IEnumerable<Ranking_Estudiantil.Models.Sanctions>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StudentsID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.punctuation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StudentsID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.punctuation)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SanctionsID">Edit</a> |
                <a asp-action="Delete" asp-route-id="@item.SanctionsID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
